apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: eva-release

commonLabels:
  app.kubernetes.io/name: eva-release
  app.kubernetes.io/component: api

resources:
- namespace.yaml
- deployment.yaml
- service.yaml
- ingress.yaml
