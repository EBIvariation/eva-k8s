apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: contig-alias

labels:
- pairs:
    app.kubernetes.io/name: contig-alias
    app.kubernetes.io/component: api
  includeSelectors: true

resources:
- deployment.yaml
- service.yaml
- ingress.yaml
