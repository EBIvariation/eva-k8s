apiVersion: apps/v1
kind: Deployment
metadata:
  name: contig-alias
spec:
  replicas: 1  # Single replica for local testing
  template:
    spec:
      securityContext:
        runAsNonRoot: false  # Allow running as root for local testing
        runAsUser: 0
        fsGroup: 0
      containers:
      - name: contig-alias
        image: ebivariation/contig-alias
        imagePullPolicy: Never  # Use local image
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "500m"
