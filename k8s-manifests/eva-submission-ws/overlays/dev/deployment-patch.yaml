apiVersion: apps/v1
kind: Deployment
metadata:
  name: eva-submission-ws
spec:
  replicas: 1  # Single replica for development
  template:
    spec:
      containers:
      - name: eva-submission-ws
        image: dockerhub.ebi.ac.uk/ebivariation/eva-submission-ws
        imagePullPolicy: Always
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
